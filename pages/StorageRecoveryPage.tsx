import React,{useState}from'react';import{useNavigate}from'react-router-dom';import Button from'../components/Button';import ArrowLeftIcon from'../components/shared/ArrowLeftIcon';import{createTransfer}from'../services/learningTransferService';import{getStorageHealth,removeInvalidLearningKeys,trimOldLearningDetails}from'../services/storageHealthService';import{copyTextToClipboard}from'../services/eiken4WorksheetService';
const StorageRecoveryPage:React.FC=()=>{const navigate=useNavigate();const[version,setVersion]=useState(0);const[status,setStatus]=useState('');const health=getStorageHealth();const backup=createTransfer();const copy=async()=>setStatus(await copyTextToClipboard(backup.link)?'復旧用リンクをコピーしました。':'コピーできませんでした。');return <div className="flex-grow bg-slate-50 p-4"><main className="mx-auto max-w-xl"><Button onClick={()=>navigate('/eiken4')} variant="ghost" size="sm"><ArrowLeftIcon className="mr-2 h-5 w-5"/>戻る</Button><header className="mt-4 rounded-3xl bg-slate-800 p-6 text-white"><p className="text-sm font-bold text-slate-300">記録を守る</p><h1 className="mt-1 text-3xl font-extrabold">保存容量・データ復旧</h1></header><section className="mt-4 rounded-3xl bg-white p-5 shadow"><div className="flex justify-between"><b>使用量</b><span>{Math.round(health.bytes/1024)}KB（目安{health.percent}%）</span></div><div className="mt-2 h-3 overflow-hidden rounded-full bg-slate-200"><div className={`h-full ${health.status==='critical'?'bg-rose-500':health.status==='warning'?'bg-amber-500':'bg-emerald-500'}`} style={{width:`${health.percent}%`}}/></div><p className="mt-3 text-sm">学習記録 {health.learningKeys}件・破損候補 {health.invalidKeys.length}件</p><Button onClick={copy} className="mt-4 w-full">先に復旧用リンクをコピー</Button></section><section className="mt-4 rounded-3xl bg-white p-5 shadow"><h2 className="font-extrabold">安全な整理</h2><p className="mt-2 text-sm text-slate-600">集計結果を残し、古い詳細履歴だけを上限内に整理します。</p><Button onClick={()=>{trimOldLearningDetails();setVersion(version+1);setStatus('古い詳細を整理しました。')}} variant="secondary" className="mt-3 w-full">古い詳細を整理</Button>{health.invalidKeys.length>0&&<><div className="mt-4 rounded-xl bg-rose-50 p-3 text-sm"><b>破損候補</b><p className="mt-1 break-all">{health.invalidKeys.join('、')}</p></div><Button onClick={()=>{const count=removeInvalidLearningKeys(health.invalidKeys);setVersion(version+1);setStatus(`${count}件の破損項目だけを分離しました。`)}} variant="danger" className="mt-3 w-full">破損項目だけを分離</Button></>}</section>{status&&<p className="mt-4 rounded-xl bg-emerald-50 p-3 text-center font-bold text-emerald-700">{status}</p>}</main></div>};export default StorageRecoveryPage;
