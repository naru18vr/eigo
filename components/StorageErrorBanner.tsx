import React,{useEffect,useState}from'react';import{useNavigate}from'react-router-dom';import Button from'./Button';
const StorageErrorBanner:React.FC=()=>{const[visible,setVisible]=useState(false);const navigate=useNavigate();useEffect(()=>{const show=()=>setVisible(true);window.addEventListener('eigo-storage-error',show);return()=>window.removeEventListener('eigo-storage-error',show)},[]);if(!visible)return null;return <div role="alert" className="fixed inset-x-3 top-3 z-[60] mx-auto max-w-md rounded-2xl border border-rose-300 bg-white p-4 shadow-2xl"><b className="text-rose-700">記録を保存できませんでした</b><p className="mt-1 text-sm">今の結果を一時退避しました。容量確認を開いてください。</p><div className="mt-3 grid grid-cols-2 gap-2"><Button onClick={()=>navigate('/storage-recovery')}>容量確認</Button><Button variant="secondary" onClick={()=>setVisible(false)}>閉じる</Button></div></div>};export default StorageErrorBanner;
